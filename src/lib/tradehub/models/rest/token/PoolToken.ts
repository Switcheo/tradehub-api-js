export interface PoolToken {
    denom: string
    amount: string
    start_time: string
    end_time: string
    duration: string
    is_locked: boolean
    commitment_power: string
    boost_factor: string
}